"""day2ops: merge-gate tooling for the Chapter 15 companion repository.

The package scores a non-deterministic agentic RAG system with deterministic
evaluation layers and turns the result into CI merge decisions. Constraints:
the default path is fully offline and deterministic; nothing in this package
requires network access, API keys, or a model server.
"""

__version__ = "0.1.0"
